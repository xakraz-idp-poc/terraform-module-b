apiVersion: backstage.io/v1alpha1
kind: Component  # Always fixed
metadata:
  name: terraform-module-b  # Repository name
  description: Description of the library component named terraform-module-b.
  tags:
    - backstage
spec:
  type: library
  lifecycle: production
  owner: idp-team
